cat
.SH Notes
.B $ cat -A ~/eg/brevity.txt
.br 
.B $ cat -vET ~/eg/brevity.txt
.br 
The -v flag shows nonprinting characters
.br
The -E flag shows a $ at the end of each line
.br
The -T flag shows the tab characters

find
.SH Notes
.B find ~ -maxdepth 0 -mtime -1
.br
Find files in ~ modified in the last 24 hours. Maxdepth determines how deeply we recurse into directories
.br

chmod
.SH Notes
This is a stub
